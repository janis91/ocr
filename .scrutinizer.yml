filter:
    excluded_paths:
        - 'js/jquery*'
        - 'l10n/*'
        - 'tests/*'
        - '*.min.js'
        - 'worker/*'

imports:
    - javascript
    - php

tools:
    external_code_coverage:
        timeout: 600 # Timeout in seconds: 2 hours
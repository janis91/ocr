filter:
    excluded_paths:
        - 'js/jquery*'
        - 'l10n/*'
        - 'tests/*'
        - '*.min.js'
        - 'worker/*'

imports:
    - javascript
    - php

tools:
    external_code_coverage:
        runs: 5
        timeout: 600 # Timeout in seconds: 2 hours
